# Both are required. Without them the contact route returns 500 rather than
# silently accepting a message it cannot send.
RESEND_API_KEY=
CONTACT_TO_EMAIL=

# Used by sitemap.ts and robots.ts. Set to the production origin at deploy time.
SITE_URL=
